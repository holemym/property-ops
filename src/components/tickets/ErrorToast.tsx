// Moved to src/components/common/ErrorToast.tsx (shared by the operator tickets surface
// and the tenant portal). Re-exported here so existing imports keep working.
export { ErrorToast } from '@/components/common/ErrorToast'
